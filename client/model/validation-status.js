const VALID = Symbol('valid config')
const INVALID = Symbol('invalid config')
const PENDING = Symbol('pending config validation')
const UNKNOWN = Symbol('unknown config')

export {
  VALID, INVALID, PENDING, UNKNOWN
}
